import sys


def main() -> None:
    """Console-script entry point (`acp-tck`). The conformance suite itself is not
    implemented yet -- this slice only ships the harness core and fixtures it will run
    against."""
    print("acp-tck: conformance suite not implemented yet", file=sys.stderr)
    raise SystemExit(2)
