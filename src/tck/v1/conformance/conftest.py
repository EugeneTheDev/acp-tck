"""No fixtures live here: `tck.v1.plugin` supplies all of them. This suite is always run with
`-p tck.v1.plugin` explicitly rather than via `pytest11` auto-registration, so nothing needs to
happen in this file.
"""
