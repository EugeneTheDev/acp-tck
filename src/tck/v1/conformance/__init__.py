"""The ACP v1 conformance suite, shipped inside the `tck` wheel.

Run with `acp-tck -- <agent command>`, or directly with
`pytest src/tck/v1/conformance -p tck.v1.plugin --tck-agent-cmd '<agent command>'`. See `AGENTS.md`
for the option reference and the requirement/test conventions.
"""
